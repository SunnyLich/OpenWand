label = "Sort this table"
hint = "Choose a selected column and preview the new row order"
prompt = "Sort the selected Excel rows while keeping every row intact."
capability = "excel.sort_range@1"
planner = "excel_plan_sort_range"
show_in_picker = false
access = ["files"]
